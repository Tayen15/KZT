<!-- Hamburger Menu Button (Mobile Only) -->
<button id="mobileSidebarToggle" class="lg:hidden fixed top-4 left-4 z-50 p-2 bg-[#2b2f38] border border-[#3a3f4b] rounded-lg hover:border-[#5865F2] transition-all">
    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
    </svg>
</button>

<!-- Mobile Overlay -->
<div id="mobileOverlay" class="lg:hidden fixed inset-0 bg-black bg-opacity-50 z-40 hidden"></div>

<!-- Full Dashboard Sidebar for Guild Pages -->
<aside
  class="w-64 bg-[#23272f] border-r border-[#3a3f4b] flex-shrink-0 flex flex-col h-screen fixed left-0 top-0 z-50 transform -translate-x-full lg:translate-x-0 transition-transform duration-300 ease-in-out"
  id="dashboardSidebar"
>
  <!-- Close Button (Mobile Only) -->
  <button id="closeSidebarBtn" class="lg:hidden absolute top-4 right-4 p-2 text-[#b9bbbe] hover:text-white transition-colors z-10">
    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
    </svg>
  </button>

  <!-- Bot Logo/Header -->
  <div class="p-4 border-b border-[#3a3f4b]">
    <a
      href="/dashboard"
      class="flex items-center gap-3 hover:opacity-80 transition-opacity"
    >
      <div
        class="w-10 h-10 bg-gradient-to-br from-[#5865F2] to-purple-600 rounded-full flex items-center justify-center font-bold text-lg"
      >
        B
      </div>
      <div>
        <h1 class="font-bold text-lg">ByteBot</h1>
        <p class="text-xs text-[#b9bbbe]">Dashboard</p>
      </div>
    </a>
  </div>

  <!-- Guild Info with Dropdown -->
  <div class="p-4 border-b border-[#3a3f4b]">
    <div class="relative">
      <!-- Current Guild Display (Clickable) -->
      <button
        onclick="toggleServerDropdown()"
        class="w-full flex items-center gap-3 p-2 rounded-lg hover:bg-[#32363f] transition-all"
      >
        <% if (guild.icon) { %>
        <img
          src="https://cdn.discordapp.com/icons/<%= guild.guildId %>/<%= guild.icon %>.png"
          alt="<%= guild.name %>"
          class="w-12 h-12 rounded-full flex-shrink-0"
        />
        <% } else { %>
        <div
          class="w-12 h-12 bg-[#5865F2] rounded-full flex items-center justify-center text-xl font-bold flex-shrink-0"
        >
          <%= guild.name.charAt(0).toUpperCase() %>
        </div>
        <% } %>
        <div class="flex-1 min-w-0 text-left">
          <h2 class="font-bold truncate"><%= guild.name %></h2>
          <p class="text-xs text-[#b9bbbe]">Server Settings</p>
        </div>
        <!-- Dropdown Icon -->
        <svg
          id="dropdownIcon"
          class="w-5 h-5 text-[#b9bbbe] transition-transform flex-shrink-0"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M19 9l-7 7-7-7"
          />
        </svg>
      </button>

      <!-- Dropdown Menu (Hidden by default) -->
      <div
        id="serverDropdown"
        class="hidden absolute top-full left-0 right-0 mt-2 bg-[#2b2f38] border border-[#3a3f4b] rounded-lg shadow-xl z-50 max-h-64 overflow-y-auto"
      >
        <div class="p-2">
          <div
            class="text-xs font-semibold text-[#b9bbbe] uppercase tracking-wider px-3 py-2"
          >
            Switch Server
          </div>
          <% if (typeof guilds !== 'undefined' && guilds && guilds.length > 0) {
          %> <% guilds.forEach(g => { %> <% if (g.guildId !== guild.guildId) {
          %> <% if (g.botInGuild) { %>
          <a
            href="/dashboard/guild/<%= g.guildId %>"
            class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-[#32363f] transition-all"
          >
            <% if (g.icon) { %>
            <img
              src="https://cdn.discordapp.com/icons/<%= g.guildId %>/<%= g.icon %>.png"
              alt="<%= g.name %>"
              class="w-8 h-8 rounded-full"
            />
            <% } else { %>
            <div
              class="w-8 h-8 bg-[#5865F2] rounded-full flex items-center justify-center text-sm font-bold"
            >
              <%= g.name.charAt(0).toUpperCase() %>
            </div>
            <% } %>
            <div class="flex-1 min-w-0">
              <p class="text-sm font-medium truncate"><%= g.name %></p>
            </div>
          </a>
          <% } else { %>
          <a
            href="https://discord.com/oauth2/authorize?client_id=<%= process.env.DISCORD_CLIENT_ID %>&permissions=8&scope=bot%20applications.commands&guild_id=<%= g.guildId %>&disable_guild_select=true"
            target="_blank"
            class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-[#32363f] transition-all border-l-2 border-yellow-500/50"
          >
            <% if (g.icon) { %>
            <img
              src="https://cdn.discordapp.com/icons/<%= g.guildId %>/<%= g.icon %>.png"
              alt="<%= g.name %>"
              class="w-8 h-8 rounded-full opacity-60"
            />
            <% } else { %>
            <div
              class="w-8 h-8 bg-[#5865F2] rounded-full flex items-center justify-center text-sm font-bold opacity-60"
            >
              <%= g.name.charAt(0).toUpperCase() %>
            </div>
            <% } %>
            <div class="flex-1 min-w-0">
              <p class="text-sm font-medium truncate text-[#b9bbbe]">
                <%= g.name %>
              </p>
              <p class="text-xs text-yellow-500">Bot not in server</p>
            </div>
          </a>
          <% } %> <% } %> <% }); %> <% } %>
          <!-- Back to Dashboard Link -->
          <div class="border-t border-[#3a3f4b] mt-2 pt-2">
            <a
              href="/dashboard"
              class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-[#32363f] text-[#5865F2] transition-all"
            >
              <svg
                class="w-5 h-5"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zM14 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zM14 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z"
                />
              </svg>
              <span class="text-sm font-medium">All Servers</span>
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script>
    function toggleServerDropdown() {
      const dropdown = document.getElementById("serverDropdown");
      const icon = document.getElementById("dropdownIcon");

      if (dropdown.classList.contains("hidden")) {
        dropdown.classList.remove("hidden");
        icon.style.transform = "rotate(180deg)";
      } else {
        dropdown.classList.add("hidden");
        icon.style.transform = "rotate(0deg)";
      }
    }

    // Close dropdown when clicking outside
    document.addEventListener("click", function (event) {
      const dropdown = document.getElementById("serverDropdown");
      const button = event.target.closest(
        'button[onclick="toggleServerDropdown()"]'
      );

      if (!button && !dropdown.contains(event.target)) {
        dropdown.classList.add("hidden");
        document.getElementById("dropdownIcon").style.transform =
          "rotate(0deg)";
      }
    });
  </script>

  <!-- Navigation -->
  <nav class="flex-1 overflow-y-auto p-2">
    <!-- Main Section -->
    <div class="mb-4">
      <h3
        class="text-xs font-semibold text-[#b9bbbe] uppercase tracking-wider px-3 mb-2"
      >
        Main
      </h3>
      <ul class="space-y-1">
        <li>
          <a
            href="/dashboard/guild/<%= guild.guildId %>"
            class="flex items-center gap-3 px-3 py-2 rounded-lg <%= currentPage === 'overview' ? 'bg-[#5865F2] text-white font-medium' : 'text-[#b9bbbe] hover:text-white hover:bg-[#32363f]' %> transition-all"
          >
            <svg
              class="w-5 h-5 flex-shrink-0"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"
              />
            </svg>
            <span class="text-sm">Overview</span>
          </a>
        </li>
      </ul>
    </div>

    <!-- Features Section -->
    <div class="mb-4">
      <h3
        class="text-xs font-semibold text-[#b9bbbe] uppercase tracking-wider px-3 mb-2"
      >
        Features
      </h3>
      <ul class="space-y-1">
        <li>
          <a
            href="/dashboard/guild/<%= guild.guildId %>/prayer"
            class="flex items-center gap-3 px-3 py-2 rounded-lg <%= currentPage === 'prayer' ? 'bg-[#5865F2] text-white font-medium' : 'text-[#b9bbbe] hover:text-white hover:bg-[#32363f]' %> transition-all"
          >
            <svg
              class="w-5 h-5 flex-shrink-0"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"
              />
            </svg>
            <span class="text-sm">Prayer Times</span>
          </a>
        </li>
        <!-- <li>
          <a
            href="/dashboard/guild/<%= guild.guildId %>/monitoring"
            class="flex items-center gap-3 px-3 py-2 rounded-lg <%= currentPage === 'monitoring' ? 'bg-[#5865F2] text-white font-medium' : 'text-[#b9bbbe] hover:text-white hover:bg-[#32363f]' %> transition-all"
          >
            <svg
              class="w-5 h-5 flex-shrink-0"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"
              />
            </svg>
            <span class="text-sm">Server Monitoring</span>
          </a>
        </li>
        <li>
          <a
            href="/dashboard/guild/<%= guild.guildId %>/rules"
            class="flex items-center gap-3 px-3 py-2 rounded-lg <%= currentPage === 'rules' ? 'bg-[#5865F2] text-white font-medium' : 'text-[#b9bbbe] hover:text-white hover:bg-[#32363f]' %> transition-all"
          >
            <svg
              class="w-5 h-5 flex-shrink-0"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"
              />
            </svg>
            <span class="text-sm">Server Rules</span>
          </a>
        </li> -->
        <li>
          <a
            href="/dashboard/guild/<%= guild.guildId %>/welcome"
            class="flex items-center gap-3 px-3 py-2 rounded-lg <%= currentPage === 'welcome' ? 'bg-[#5865F2] text-white font-medium' : 'text-[#b9bbbe] hover:text-white hover:bg-[#32363f]' %> transition-all"
          >
            <svg
              class="w-5 h-5 flex-shrink-0"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"
              />
            </svg>
            <span class="text-sm">Welcome Messages</span>
          </a>
        </li>
      </ul>
    </div>

    <!-- Coming Soon Section -->
    <div>
      <h3
        class="text-xs font-semibold text-[#b9bbbe] uppercase tracking-wider px-3 mb-2"
      >
        Coming Soon
      </h3>
      <ul class="space-y-1">
        <li>
          <div
            class="flex items-center gap-3 px-3 py-2 text-[#b9bbbe]/50 cursor-not-allowed"
          >
            <svg
              class="w-5 h-5 flex-shrink-0"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M12 8v13m0-13V6a2 2 0 112 2h-2zm0 0V5.5A2.5 2.5 0 109.5 8H12zm-7 4h14M5 12a2 2 0 110-4h14a2 2 0 110 4M5 12v7a2 2 0 002 2h10a2 2 0 002-2v-7"
              />
            </svg>
            <span class="text-sm">Auto Roles</span>
          </div>
        </li>
        <li>
          <div
            class="flex items-center gap-3 px-3 py-2 text-[#b9bbbe]/50 cursor-not-allowed"
          >
            <svg
              class="w-5 h-5 flex-shrink-0"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M13 10V3L4 14h7v7l9-11h-7z"
              />
            </svg>
            <span class="text-sm">Custom Commands</span>
          </div>
        </li>
      </ul>
    </div>
  </nav>

  <!-- User Info at Bottom -->
  <div class="p-4 border-t border-[#3a3f4b]">
    <div class="flex items-center justify-between">
      <div class="flex items-center gap-3 flex-1 min-w-0">
        <% if (user.avatar) { %>
        <img
          src="https://cdn.discordapp.com/avatars/<%= user.discordId %>/<%= user.avatar %>.png"
          alt="<%= user.username %>"
          class="w-8 h-8 rounded-full"
        />
        <% } else { %>
        <div
          class="w-8 h-8 bg-[#5865F2] rounded-full flex items-center justify-center text-sm font-bold"
        >
          <%= user.username.charAt(0).toUpperCase() %>
        </div>
        <% } %>
        <div class="flex-1 min-w-0">
          <p class="text-sm font-semibold truncate"><%= user.username %></p>
          <p class="text-xs text-[#b9bbbe]">Logged in</p>
        </div>
      </div>
      <a
        href="/auth/logout"
        class="text-[#b9bbbe] hover:text-[#ED4245] transition-colors p-2 hover:bg-[#32363f] rounded-lg"
        title="Logout"
      >
        <svg
          class="w-5 h-5"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"
          />
        </svg>
      </a>
    </div>
  </div>
</aside>

<!-- Mobile Sidebar Toggle Script -->
<script>
  // Mobile sidebar toggle
  const mobileSidebarToggle = document.getElementById('mobileSidebarToggle');
  const dashboardSidebar = document.getElementById('dashboardSidebar');
  const mobileOverlay = document.getElementById('mobileOverlay');
  const closeSidebarBtn = document.getElementById('closeSidebarBtn');

  function openMobileSidebar() {
    dashboardSidebar.classList.remove('-translate-x-full');
    mobileOverlay.classList.remove('hidden');
    document.body.style.overflow = 'hidden'; // Prevent scroll
  }

  function closeMobileSidebar() {
    dashboardSidebar.classList.add('-translate-x-full');
    mobileOverlay.classList.add('hidden');
    document.body.style.overflow = ''; // Restore scroll
  }

  if (mobileSidebarToggle) {
    mobileSidebarToggle.addEventListener('click', openMobileSidebar);
  }

  if (closeSidebarBtn) {
    closeSidebarBtn.addEventListener('click', closeMobileSidebar);
  }

  if (mobileOverlay) {
    mobileOverlay.addEventListener('click', closeMobileSidebar);
  }

  // Close sidebar on ESC key
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && !dashboardSidebar.classList.contains('-translate-x-full')) {
      closeMobileSidebar();
    }
  });

  // Close sidebar when switching to desktop view
  window.addEventListener('resize', () => {
    if (window.innerWidth >= 1024) { // lg breakpoint
      closeMobileSidebar();
    }
  });
</script>
